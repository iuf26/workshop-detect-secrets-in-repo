# project-detect-secrets-in-repo
This is a repository that stores a project that uses MCP servers and Microsoft Agent Framework to detect tokens, api keys, PII or any other secrets that might be found in a repostiory code base to prevent secrets leak.
